type DateKey = 'DATETIME' | 'DATE' | 'DATE_TEXT' | 'MONTH_NAME' | 'MONTH_NUMBER'

type DateFormat = 'DD-MM-YYYY'
type DateTextFormat = 'LL'
type DateTimeFormat = 'DD-MM-YYYY HH:mm:ss'
type MonthNameFormat = 'MMMM'
type MonthNumberFormat = 'MM'

export { DateKey, DateFormat, DateTextFormat, DateTimeFormat, MonthNameFormat, MonthNumberFormat }
