type DateKey = 'DATETIME' | 'DATE' | 'DATE_TEXT' | 'MONTH_NAME' | 'MONTH_NUMBER'
type Date = 'DD-MM-YYYY' | 'LL'
type DateTime = 'DD-MM-YYYY HH:mm:ss'
type Month = 'MMMM' | 'MM'

export { DateKey, Date, DateTime, Month }
